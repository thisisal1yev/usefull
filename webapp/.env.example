# Base URL of the usfull server (no trailing slash).
# Local dev: point at the local server.
# Production: set the public API domain in Vercel → Project Settings →
# Environment Variables (not in this file), then redeploy.
VITE_API_URL=http://localhost:3000
